---
phase: 05-run-management
plan: 01
type: execute
---

<objective>
Add polish features: reset save button, controls help text, and run counter during gameplay.

Purpose: Complete the run management UX with helpful text and reset option. These are quality-of-life features that improve player experience.

Output: Reset button in hub, controls help visible, run counter during gameplay.
</objective>

<context>
@.planning/phases/05-run-management/05-RESEARCH.md
@src/scenes/HubScene.ts
@src/scenes/GameScene.ts

**Existing systems:**
- MetaProgressionManager.resetSave() - already exists
- Lifetime stats tracking - already works
- HubScene with all panels - needs reset button
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reset save button to HubScene</name>
  <files>src/scenes/HubScene.ts</files>
  <action>Add a small "Reset Progress" text button at bottom-left of HubScene. On click, call meta.resetSave() and refresh display. Use red color (#ff4444) to indicate danger. Optional: Add simple confirmation (click twice to confirm) or just allow instant reset for faster iteration.</action>
  <verify>npm run dev, click reset, progress clears</verify>
  <done>Reset button works, clears all progress</done>
</task>

<task type="auto">
  <name>Task 2: Add controls help to HubScene</name>
  <files>src/scenes/HubScene.ts</files>
  <action>Add controls help text below the start button or in a corner: "Controls: WASD/Arrows to move, 1-5 to use items, R to return to hub". Small gray text that doesn't clutter but helps new players.</action>
  <verify>npm run dev, controls visible in hub</verify>
  <done>Controls help text visible in hub</done>
</task>

<task type="auto">
  <name>Task 3: Add run counter to GameScene</name>
  <files>src/scenes/GameScene.ts</files>
  <action>Add small text in corner showing current run number: "Run #X" using meta.getLifetime().runsStarted. Helps player track how many attempts they've made. Position at top-left near player stats or top-center.</action>
  <verify>npm run dev, run number shows during gameplay</verify>
  <done>Run counter visible during gameplay</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors
- [ ] Reset button works and clears progress
- [ ] Controls help visible in hub
- [ ] Run counter visible during gameplay
- [ ] All features are unobtrusive polish
</verification>

<success_criteria>
- Reset save functional
- Controls help displayed
- Run counter shown
- Phase 5 complete - all polish done
</success_criteria>
